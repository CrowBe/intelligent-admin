// Re-export Card components for clean imports
// Maintains shadcn-ui compatibility: import { Card, CardHeader, CardContent } from '@/components/ui/card'
export { Card, CardHeader, CardFooter, CardTitle, CardAction, CardDescription, CardContent } from './Card';
export type { CardProps, CardHeaderProps, CardTitleProps, CardDescriptionProps, CardActionProps, CardContentProps, CardFooterProps } from './Card';
